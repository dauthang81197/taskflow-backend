apiVersion: v1
kind: Service
metadata:
  name: taskflow-backend-service-dev
spec:
  type: NodePort
  selector:
    app: taskflow-backend-dev
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30010
